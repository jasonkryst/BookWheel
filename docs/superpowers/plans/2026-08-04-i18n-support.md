# I18N Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English/Spanish/Polish localization to BookWheel's frontend UI and the handful of server-generated error messages, with a manual language toggle persisted per-browser.

**Architecture:** Frontend catalog file (`wwwroot/js/i18n.js`) exposes `window.BookWheelI18n.t(key, params)`; `index.html` gets `data-i18n*` attributes plus a language toggle; `app.js` calls `t()` instead of hardcoding strings and sends `Accept-Language` on every API call. Backend maps the small set of known English response strings to resx-backed translations via ASP.NET Core's `RequestLocalization` middleware, without touching the exception-throwing call sites.

**Tech Stack:** Vanilla JS (no bundler/module system — matches existing `app.js`), ASP.NET Core 8 `IStringLocalizer` + `.resx`, xUnit.

## Global Constraints

- Design doc: `docs/superpowers/specs/2026-08-04-i18n-support-design.md` — follow it for architecture decisions.
- Supported locales: `en` (default), `es`, `pl`. No other locales in scope.
- No build step exists in this project — all frontend files are served as-is from `wwwroot/`. Do not introduce a bundler.
- Match the existing test style in `BookWheel.Tests`: explicit `// Positive:` / `// Negative:` comments above `Assert.Contains`/`Assert.DoesNotContain` checks (see `Frontend_Styles_Should_Include_High_Contrast_Theme_Variables` in `BookWheelFrontendTests.cs` for the pattern).
- Untranslated/unmapped strings must fail open (render in English or as the raw key) — never throw.
- Bump `BookWheel/BookWheel.csproj`'s `InformationalVersion` from `1.7.0` to `1.8.0` only in the final task, after all tests pass.
- Every C# file in this repo uses tabs for indentation (see `Program.cs`, `AuthController.cs`); every JS/HTML/CSS file uses 2-space indentation (see `app.js`, `index.html`). Match whichever you're editing.

---

## Appendix A — Backend message map

`ApiMessageLocalizer` (Task 1) maps each of these exact English strings (as currently thrown/returned, verbatim) to a resx key. Use this table as the literal content for the `KeysByEnglishMessage` dictionary and the three `.resx` files.

| English (dictionary key, exact match) | Resx key | es | pl |
|---|---|---|---|
| Invalid username or password. | InvalidCredentials | Nombre de usuario o contraseña incorrectos. | Nieprawidłowa nazwa użytkownika lub hasło. |
| The password reset link is invalid or has expired. | LinkInvalidOrExpired | El enlace de restablecimiento de contraseña no es válido o ha caducado. | Link resetowania hasła jest nieprawidłowy lub wygasł. |
| Book title is required. | BookTitleRequired | El título del libro es obligatorio. | Tytuł książki jest wymagany. |
| A valid token and password are required. | TokenAndPasswordRequired | Se requieren un token válido y una contraseña. | Wymagany jest prawidłowy token i hasło. |
| Password reset token data is corrupted and has been quarantined. Restore App_Data from backup. | ResetTokenDataCorrupted | Los datos del token de restablecimiento están dañados y se han puesto en cuarentena. Restaura App_Data desde una copia de seguridad. | Dane tokenu resetowania są uszkodzone i zostały poddane kwarantannie. Przywróć App_Data z kopii zapasowej. |
| An account already exists. | AccountAlreadyExists | Ya existe una cuenta. | Konto już istnieje. |
| Username and password are required. | UsernameAndPasswordRequired | El nombre de usuario y la contraseña son obligatorios. | Nazwa użytkownika i hasło są wymagane. |
| Create the initial account first. | CreateInitialAccountFirst | Primero crea la cuenta inicial. | Najpierw utwórz konto początkowe. |
| Username is required. | UsernameRequired | El nombre de usuario es obligatorio. | Nazwa użytkownika jest wymagana. |
| Username already exists. | UsernameAlreadyExists | El nombre de usuario ya existe. | Nazwa użytkownika już istnieje. |
| User not found. | UserNotFound | Usuario no encontrado. | Nie znaleziono użytkownika. |
| At least one administrator account is required. | AtLeastOneAdminRequired | Se requiere al menos una cuenta de administrador. | Wymagane jest co najmniej jedno konto administratora. |
| The first account cannot be removed. | FirstAccountCannotBeRemoved | La primera cuenta no se puede eliminar. | Pierwszego konta nie można usunąć. |
| User not found for this reset link. | UserNotFoundForResetLink | No se encontró ningún usuario para este enlace de restablecimiento. | Nie znaleziono użytkownika dla tego linku resetującego. |
| Credential data is corrupted and has been quarantined. Restore App_Data from backup. | CredentialDataCorrupted | Los datos de credenciales están dañados y se han puesto en cuarentena. Restaura App_Data desde una copia de seguridad. | Dane uwierzytelniające są uszkodzone i zostały poddane kwarantannie. Przywróć App_Data z kopii zapasowej. |
| Book not found. | BookNotFound | Libro no encontrado. | Nie znaleziono książki. |
| No books are available in the wheel. | NoBooksAvailable | No hay libros disponibles en la rueda. | Brak dostępnych książek na kole. |
| Book data is corrupted and has been quarantined. Restore App_Data from backup. | BookDataCorrupted | Los datos de los libros están dañados y se han puesto en cuarentena. Restaura App_Data desde una copia de seguridad. | Dane książek są uszkodzone i zostały poddane kwarantannie. Przywróć App_Data z kopii zapasowej. |
| Administrators can only update other user accounts. | AdminsCanOnlyUpdateOthers | Los administradores solo pueden actualizar otras cuentas de usuario. | Administratorzy mogą aktualizować tylko inne konta użytkowników. |
| Administrators can only generate reset links for other user accounts. | AdminsCanOnlyGenerateResetLinksForOthers | Los administradores solo pueden generar enlaces de restablecimiento para otras cuentas de usuario. | Administratorzy mogą generować linki resetujące tylko dla innych kont użytkowników. |
| Administrators can only remove other user accounts. | AdminsCanOnlyRemoveOthers | Los administradores solo pueden eliminar otras cuentas de usuario. | Administratorzy mogą usuwać tylko inne konta użytkowników. |

## Appendix B — Frontend translation catalog

Literal content for `TRANSLATIONS` in `wwwroot/js/i18n.js` (Task 3), grouped by namespace (nested object: `TRANSLATIONS.en.auth.loginTitle`, etc.). `{param}` tokens are interpolated by `t()`.

### common
| key | en | es | pl |
|---|---|---|---|
| skipToContent | Skip to main content | Saltar al contenido principal | Przejdź do głównej treści |
| cancel | Cancel | Cancelar | Anuluj |
| save | Save | Guardar | Zapisz |
| saving | Saving... | Guardando... | Zapisywanie... |
| close | Close | Cerrar | Zamknij |
| clear | Clear | Borrar | Wyczyść |
| remove | Remove | Eliminar | Usuń |
| creating | Creating... | Creando... | Tworzenie... |
| generating | Generating... | Generando... | Generowanie... |
| requestFailed | Request failed. | Error en la solicitud. | Żądanie nie powiodło się. |
| versionLabel | Version: {version} | Versión: {version} | Wersja: {version} |
| unknownVersion | unknown | desconocida | nieznana |
| connectionError | Cannot connect to the server. Make sure the app is running, then try again. | No se puede conectar con el servidor. Asegúrate de que la aplicación esté en ejecución e inténtalo de nuevo. | Nie można połączyć się z serwerem. Upewnij się, że aplikacja działa, a następnie spróbuj ponownie. |

### app
| key | en | es | pl |
|---|---|---|---|
| title | Book Wheel | Book Wheel | Book Wheel |

### auth
| key | en | es | pl |
|---|---|---|---|
| loginTitle | Book Wheel Login | Inicio de sesión de Book Wheel | Logowanie do Book Wheel |
| loginSubtitle | Log in with your existing account. | Inicia sesión con tu cuenta existente. | Zaloguj się na swoje istniejące konto. |
| loginSubmit | Log in | Iniciar sesión | Zaloguj się |
| usernameLabel | Username | Nombre de usuario | Nazwa użytkownika |
| passwordLabel | Password | Contraseña | Hasło |
| logoutBtn | Log out | Cerrar sesión | Wyloguj się |
| setupTitle | Create your Book Wheel account | Crea tu cuenta de Book Wheel | Utwórz konto Book Wheel |
| setupSubtitle | No account exists yet. Create one to begin. | Todavía no existe ninguna cuenta. Crea una para empezar. | Nie istnieje jeszcze żadne konto. Utwórz jedno, aby rozpocząć. |
| setupSubmit | Create account | Crear cuenta | Utwórz konto |
| setPasswordTitle | Set your Book Wheel password | Establece tu contraseña de Book Wheel | Ustaw hasło do Book Wheel |
| setPasswordSubtitle | Use the secure reset link from your administrator. | Usa el enlace seguro de restablecimiento proporcionado por tu administrador. | Użyj bezpiecznego linku resetującego otrzymanego od administratora. |
| newPasswordLabel | New password | Nueva contraseña | Nowe hasło |
| confirmPasswordLabel | Confirm password | Confirmar contraseña | Potwierdź hasło |
| setPasswordSubmit | Set password | Establecer contraseña | Ustaw hasło |
| validatingLinkTitle | Validate password reset link | Validando enlace de restablecimiento de contraseña | Weryfikacja linku resetowania hasła |
| validatingLinkSubtitle | Checking your secure reset link... | Comprobando tu enlace seguro... | Sprawdzanie bezpiecznego linku... |
| setPasswordForUser | Set password for {username} | Establecer contraseña para {username} | Ustaw hasło dla {username} |
| defaultAccountName | your account | tu cuenta | Twoje konto |
| linkExpiresAt | This link expires at {datetime}. | Este enlace caduca el {datetime}. | Ten link wygasa {datetime}. |
| linkExpires24h | This link expires in 24 hours. | Este enlace caduca en 24 horas. | Ten link wygasa za 24 godziny. |
| linkInvalidError | The password reset link is invalid or has expired. | El enlace de restablecimiento de contraseña no es válido o ha caducado. | Link resetowania hasła jest nieprawidłowy lub wygasł. |
| linkValidatedToast | Reset link validated. | Enlace de restablecimiento validado. | Link resetujący został zweryfikowany. |
| credentialsRequiredError | Username and password are required. | El nombre de usuario y la contraseña son obligatorios. | Nazwa użytkownika i hasło są wymagane. |
| creatingAccountBusy | Creating account... | Creando cuenta... | Tworzenie konta... |
| loggingInBusy | Logging in... | Iniciando sesión... | Logowanie... |
| accountCreatedToast | Account created and signed in. | Cuenta creada e inicio de sesión realizado. | Konto utworzone, zalogowano pomyślnie. |
| signedInToast | Signed in successfully. | Sesión iniciada correctamente. | Zalogowano pomyślnie. |
| signedOutToast | Signed out. | Sesión cerrada. | Wylogowano. |
| resetTokenMissingError | Reset token is missing. | Falta el token de restablecimiento. | Brak tokenu resetowania. |
| bothPasswordsRequiredError | Both password fields are required. | Ambos campos de contraseña son obligatorios. | Oba pola hasła są wymagane. |
| passwordTooShortError | Password must be at least 8 characters. | La contraseña debe tener al menos 8 caracteres. | Hasło musi mieć co najmniej 8 znaków. |
| passwordsMismatchError | Passwords do not match. | Las contraseñas no coinciden. | Hasła nie są zgodne. |
| passwordUpdatedMessage | Password updated. You can now log in with your new password. | Contraseña actualizada. Ya puedes iniciar sesión con tu nueva contraseña. | Hasło zaktualizowane. Możesz teraz zalogować się nowym hasłem. |
| passwordUpdatedToast | Password updated successfully. | Contraseña actualizada correctamente. | Hasło zostało pomyślnie zaktualizowane. |

### theme
| key | en | es | pl |
|---|---|---|---|
| switchThemeLabel | Switch theme | Cambiar tema | Zmień motyw |
| switchTo | Switch to {label} | Cambiar a {label} | Przełącz na {label} |
| dark | Dark mode | Modo oscuro | Tryb ciemny |
| light | Light mode | Modo claro | Tryb jasny |
| highContrast | High contrast mode | Modo de alto contraste | Tryb wysokiego kontrastu |

### lang
| key | en | es | pl |
|---|---|---|---|
| switchLanguageLabel | Change language | Cambiar idioma | Zmień język |
| switchLanguageAria | Change language, current: {language} | Cambiar idioma, actual: {language} | Zmień język, obecny: {language} |

(Language names themselves — "English"/"Español"/"Polski" — are NOT translated per-locale; they come from a fixed `LANGUAGE_NAMES` map in `i18n.js`, always shown in each language's own native name. See Task 3.)

### wheel
| key | en | es | pl |
|---|---|---|---|
| heading | Spin wheel | Rueda giratoria | Koło losujące |
| canvasAriaLabel | Book selection wheel. Press Enter or Space to spin. | Rueda de selección de libros. Pulsa Intro o Espacio para girar. | Koło wyboru książki. Naciśnij Enter lub Spację, aby zakręcić. |
| spinBtn | Spin | Girar | Zakręć |
| spinningBusy | Spinning... | Girando... | Kręcenie... |
| emptyStatus | Wheel is empty. Add books to spin. | La rueda está vacía. Añade libros para girar. | Koło jest puste. Dodaj książki, aby zakręcić. |
| summaryCount | Wheel has {count} books. | La rueda tiene {count} libros. | Koło zawiera {count} książek. |
| addBooksPrompt | Add books to spin | Añade libros para girar | Dodaj książki, aby zakręcić |
| emptyPrompt | Add your first book to begin spinning. | Añade tu primer libro para empezar a girar. | Dodaj pierwszą książkę, aby zacząć losowanie. |
| addBooksError | Add books to spin. | Añade libros para girar. | Dodaj książki, aby zakręcić. |
| selectionNotFoundError | Selected book was not found on the current wheel. | El libro seleccionado no se encontró en la rueda actual. | Wybrana książka nie została znaleziona na bieżącym kole. |
| lastSelected | Last selected: {title} | Última selección: {title} | Ostatnio wybrano: {title} |
| selectedToast | Selected: {title} | Seleccionado: {title} | Wybrano: {title} |

### books
| key | en | es | pl |
|---|---|---|---|
| heading | Active books | Libros activos | Aktywne książki |
| titleLabel | Book title | Título del libro | Tytuł książki |
| titlePlaceholder | Book title | Título del libro | Tytuł książki |
| addBtn | Add | Añadir | Dodaj |
| prevPage | Previous | Anterior | Poprzednia |
| nextPage | Next | Siguiente | Następna |
| pageInfo | Page {current} of {total} | Página {current} de {total} | Strona {current} z {total} |
| countOne | 1 book total | 1 libro en total | 1 książka łącznie |
| countOther | {count} books total | {count} libros en total | {count} książek łącznie |
| editDialogTitle | Edit book title | Editar título del libro | Edytuj tytuł książki |
| deleteDialogTitle | Remove book | Eliminar libro | Usuń książkę |
| titleRequiredError | Book title is required. | El título del libro es obligatorio. | Tytuł książki jest wymagany. |
| titleEmptyError | Title cannot be empty. | El título no puede estar vacío. | Tytuł nie może być pusty. |
| updatedMessage | Book updated. | Libro actualizado. | Książka zaktualizowana. |
| updatedToast | Book title updated. | Título del libro actualizado. | Tytuł książki zaktualizowany. |
| removeConfirmMessage | Remove "{title}" from the active list? | ¿Eliminar "{title}" de la lista activa? | Usunąć „{title}” z listy aktywnych? |
| removedMessage | Book removed from the active list. | Libro eliminado de la lista activa. | Książka usunięta z listy aktywnych. |
| removedToast | Book removed. | Libro eliminado. | Książka usunięta. |
| addedMessage | Book added. | Libro añadido. | Książka dodana. |
| addedToast | Book added to active list. | Libro añadido a la lista activa. | Książka dodana do listy aktywnych. |
| emptyHeading | No books yet | Todavía no hay libros | Brak książek |
| emptyHint1 | Add your first title above to populate the wheel. | Añade tu primer título arriba para llenar la rueda. | Dodaj pierwszy tytuł powyżej, aby zapełnić koło. |
| emptyHint2 | Tip: after adding books, press the Spin button or hit Enter on the wheel. | Consejo: después de añadir libros, pulsa el botón Girar o presiona Intro sobre la rueda. | Wskazówka: po dodaniu książek naciśnij przycisk Zakręć lub Enter na kole. |
| editTitleHint | Edit this title | Editar este título | Edytuj ten tytuł |
| editAria | Edit book title: {title} | Editar título del libro: {title} | Edytuj tytuł książki: {title} |
| removeHint | Remove from active list | Eliminar de la lista activa | Usuń z listy aktywnych |
| removeAria | Remove book: {title} | Eliminar libro: {title} | Usuń książkę: {title} |

### users
| key | en | es | pl |
|---|---|---|---|
| manageUsersBtn | Manage users | Gestionar usuarios | Zarządzaj użytkownikami |
| greeting | Hello, {username} | Hola, {username} | Witaj, {username} |
| managementDialogTitle | User management | Gestión de usuarios | Zarządzanie użytkownikami |
| adminWorkspaceKicker | Admin workspace | Espacio de administración | Panel administratora |
| managementDescription | Create users, assign administrator access, and update account details in one place. | Crea usuarios, asigna acceso de administrador y actualiza los datos de las cuentas en un solo lugar. | Twórz użytkowników, przyznawaj dostęp administratora i aktualizuj dane kont w jednym miejscu. |
| createUserHeading | Create user | Crear usuario | Utwórz użytkownika |
| newAccountTag | New account | Cuenta nueva | Nowe konto |
| createUserDescription | Create the account, then share the generated setup link so the user can set their own password. | Crea la cuenta y comparte el enlace de configuración generado para que el usuario establezca su propia contraseña. | Utwórz konto, a następnie udostępnij wygenerowany link konfiguracyjny, aby użytkownik mógł ustawić własne hasło. |
| administratorLabel | Administrator | Administrador | Administrator |
| createUserBtn | Create user | Crear usuario | Utwórz użytkownika |
| existingUsersHeading | Existing users | Usuarios existentes | Istniejący użytkownicy |
| directoryTag | Directory | Directorio | Katalog |
| updateDescription | Administrators can update other user accounts. Your own account is read-only here. | Los administradores pueden actualizar otras cuentas de usuario. Tu propia cuenta es de solo lectura aquí. | Administratorzy mogą aktualizować inne konta użytkowników. Twoje własne konto jest tutaj tylko do odczytu. |
| searchLabel | Search users | Buscar usuarios | Szukaj użytkowników |
| searchPlaceholder | Filter by username | Filtrar por nombre de usuario | Filtruj według nazwy użytkownika |
| zeroUsers | 0 users | 0 usuarios | 0 użytkowników |
| totalUsers | {count} users | {count} usuarios | {count} użytkowników |
| filteredUsers | {filtered} of {total} users | {filtered} de {total} usuarios | {filtered} z {total} użytkowników |
| noMatchFilter | No users match this filter. | Ningún usuario coincide con este filtro. | Żaden użytkownik nie pasuje do tego filtra. |
| roleAdmin | Administrator | Administrador | Administrator |
| roleStandard | Standard user | Usuario estándar | Użytkownik standardowy |
| createdOn | Created {date} | Creado el {date} | Utworzono {date} |
| createdUnavailable | Created date unavailable | Fecha de creación no disponible | Data utworzenia niedostępna |
| adminCheckbox | Admin | Admin | Admin |
| disabledCheckbox | Disabled | Deshabilitado | Wyłączony |
| requireResetCheckbox | Require reset | Requerir restablecimiento | Wymagaj resetu |
| lockedCheckbox | Locked | Bloqueado | Zablokowany |
| saveChangesAria | Save changes for {username} | Guardar cambios de {username} | Zapisz zmiany dla {username} |
| removeUserAria | Remove user {username} | Eliminar usuario {username} | Usuń użytkownika {username} |
| generateResetLinkBtn | Generate reset link | Generar enlace de restablecimiento | Wygeneruj link resetujący |
| generateResetLinkAria | Generate password reset link for {username} | Generar enlace de restablecimiento de contraseña para {username} | Wygeneruj link resetowania hasła dla {username} |
| ownAccountHint | Use account settings for your own account updates. | Usa la configuración de la cuenta para actualizar tu propia cuenta. | Użyj ustawień konta, aby zaktualizować własne konto. |
| cannotResetOwnHint | You cannot generate a reset link for the active account. | No puedes generar un enlace de restablecimiento para la cuenta activa. | Nie możesz wygenerować linku resetującego dla aktywnego konta. |
| cannotRemoveOwnHint | You cannot remove the active account. | No puedes eliminar la cuenta activa. | Nie możesz usunąć aktywnego konta. |
| cannotRemoveFirstHint | The first account cannot be removed. | La primera cuenta no se puede eliminar. | Pierwszego konta nie można usunąć. |
| currentUserSuffix | {username} (you) | {username} (tú) | {username} (Ty) |
| usernameRequiredError | Username is required. | El nombre de usuario es obligatorio. | Nazwa użytkownika jest wymagana. |
| updatedUserMessage | Updated user {username}. | Usuario {username} actualizado. | Zaktualizowano użytkownika {username}. |
| generatedResetLinkMessage | Generated a secure reset link for {username}. | Se generó un enlace seguro de restablecimiento para {username}. | Wygenerowano bezpieczny link resetujący dla {username}. |
| generatedResetLinkToast | Reset link generated for {username}. | Enlace de restablecimiento generado para {username}. | Wygenerowano link resetujący dla {username}. |
| createdUserMessage | Created user {username}. | Usuario {username} creado. | Utworzono użytkownika {username}. |
| createdUserToast | Created user {username}. | Usuario {username} creado. | Utworzono użytkownika {username}. |
| deleteDialogTitle | Remove user account | Eliminar cuenta de usuario | Usuń konto użytkownika |
| deleteWarning | This action permanently removes the account and all books assigned to this user. | Esta acción elimina permanentemente la cuenta y todos los libros asignados a este usuario. | Ta czynność trwale usuwa konto i wszystkie książki przypisane do tego użytkownika. |
| removeUserBtn | Remove user | Eliminar usuario | Usuń użytkownika |
| deleteConfirmMessage | Remove user "{username}" and all of their books? | ¿Eliminar al usuario "{username}" y todos sus libros? | Usunąć użytkownika „{username}” i wszystkie jego książki? |
| removedUserMessage | Removed user {username}. Deleted {count} books. | Usuario {username} eliminado. Se eliminaron {count} libros. | Usunięto użytkownika {username}. Usunięto {count} książek. |
| removedUserToast | Removed user {username}. | Usuario {username} eliminado. | Usunięto użytkownika {username}. |
| resetLinkDialogTitle | Password reset link | Enlace de restablecimiento de contraseña | Link resetowania hasła |
| resetLinkShareLabel | Share this link with the user | Comparte este enlace con el usuario | Udostępnij ten link użytkownikowi |
| copyLinkBtn | Copy link | Copiar enlace | Skopiuj link |
| resetLinkCreated | Reset link created for {username}. {expiryText} | Enlace de restablecimiento creado para {username}. {expiryText} | Utworzono link resetujący dla {username}. {expiryText} |
| linkExpiresAt | Link expires {datetime}. | El enlace caduca el {datetime}. | Link wygasa {datetime}. |
| linkExpires24h | Link expires in 24 hours. | El enlace caduca en 24 horas. | Link wygasa za 24 godziny. |
| noResetLinkError | No reset link is available. | No hay ningún enlace de restablecimiento disponible. | Brak dostępnego linku resetującego. |
| linkCopiedMessage | Reset link copied to clipboard. | Enlace de restablecimiento copiado al portapapeles. | Link resetujący skopiowany do schowka. |
| copyFailedMessage | Copy failed. Select and copy the link manually. | Error al copiar. Selecciona y copia el enlace manualmente. | Kopiowanie nie powiodło się. Zaznacz i skopiuj link ręcznie. |

### transfer
| key | en | es | pl |
|---|---|---|---|
| importExportLabel | Import or export books | Importar o exportar libros | Importuj lub eksportuj książki |
| dialogTitle | Import / Export books | Importar / Exportar libros | Importuj / eksportuj książki |
| importTab | Import | Importar | Import |
| exportTab | Export | Exportar | Eksport |
| importDescription | Import JSON data and merge into your list. Existing title matches are skipped case-insensitively. | Importa datos JSON y combínalos con tu lista. Los títulos coincidentes se omiten sin distinguir mayúsculas y minúsculas. | Zaimportuj dane JSON i połącz je z listą. Pasujące tytuły są pomijane bez rozróżniania wielkości liter. |
| chooseFileLabel | Choose JSON file | Elegir archivo JSON | Wybierz plik JSON |
| importFileBtn | Import File | Importar archivo | Importuj plik |
| exportDescription | Download your current books as a JSON file. | Descarga tus libros actuales como archivo JSON. | Pobierz swoje bieżące książki jako plik JSON. |
| downloadBtn | Download JSON | Descargar JSON | Pobierz JSON |
| invalidJsonError | Invalid JSON format. Use an array or an object with a books array. | Formato JSON no válido. Usa un array o un objeto con un array de libros. | Nieprawidłowy format JSON. Użyj tablicy lub obiektu z tablicą książek. |
| chooseFileError | Choose a JSON file to import. | Elige un archivo JSON para importar. | Wybierz plik JSON do zaimportowania. |
| emptyFileError | The selected file is empty. | El archivo seleccionado está vacío. | Wybrany plik jest pusty. |
| noTitlesError | No valid titles found in JSON. | No se encontraron títulos válidos en el JSON. | Nie znaleziono prawidłowych tytułów w pliku JSON. |
| importCompleteMessage | Import complete. Added {added}, skipped {skipped} matches. | Importación completa. Añadidos {added}, omitidas {skipped} coincidencias. | Import zakończony. Dodano {added}, pominięto {skipped} dopasowań. |
| exportStartedMessage | Export file download started. | Se inició la descarga del archivo de exportación. | Rozpoczęto pobieranie pliku eksportu. |

---

## Task 1: Backend — `ApiMessageLocalizer` + resx resources

**Files:**
- Create: `BookWheel/Resources/SharedErrors.cs`
- Create: `BookWheel/Resources/SharedErrors.resx`
- Create: `BookWheel/Resources/SharedErrors.es.resx`
- Create: `BookWheel/Resources/SharedErrors.pl.resx`
- Create: `BookWheel/Services/ApiMessageLocalizer.cs`
- Test: `BookWheel.Tests/Services/ApiMessageLocalizerTests.cs`

**Interfaces:**
- Produces: `BookWheel.Services.ApiMessageLocalizer` with constructor `ApiMessageLocalizer(IStringLocalizer<SharedErrors> localizer)` and method `string Localize(string englishMessage)`.
- Produces: `BookWheel.Resources.SharedErrors` (empty marker class, resx type anchor).

- [ ] **Step 1: Create the resx marker class**

```csharp
// BookWheel/Resources/SharedErrors.cs
namespace BookWheel.Resources;

public sealed class SharedErrors
{
}
```

- [ ] **Step 2: Create the three resx files**

Standard .NET resx XML schema. Create `BookWheel/Resources/SharedErrors.resx` with one `<data name="{ResxKey}" xml:space="preserve"><value>{English}</value></data>` entry per row in Appendix A (21 entries), using this file skeleton:

```xml
<?xml version="1.0" encoding="utf-8"?>
<root>
  <resheader name="resmimetype">
    <value>text/microsoft-resx</value>
  </resheader>
  <resheader name="version">
    <value>2.0</value>
  </resheader>
  <resheader name="reader">
    <value>System.Resources.ResXResourceReader, System.Windows.Forms</value>
  </resheader>
  <resheader name="writer">
    <value>System.Resources.ResXResourceWriter, System.Windows.Forms</value>
  </resheader>
  <data name="InvalidCredentials" xml:space="preserve">
    <value>Invalid username or password.</value>
  </data>
  <!-- ...remaining 20 entries from Appendix A, English column... -->
</root>
```

Create `BookWheel/Resources/SharedErrors.es.resx` with the same 21 `<data>` entries, values from Appendix A's `es` column. Create `BookWheel/Resources/SharedErrors.pl.resx` with the same 21 entries, values from Appendix A's `pl` column. All three files use the identical set of 21 `name` attributes (the Resx key column).

- [ ] **Step 3: Write the failing localizer tests**

```csharp
// BookWheel.Tests/Services/ApiMessageLocalizerTests.cs
using System.Globalization;
using BookWheel.Resources;
using BookWheel.Services;
using Microsoft.Extensions.Localization;

namespace BookWheel.Tests.Services;

public sealed class ApiMessageLocalizerTests
{
    private static ApiMessageLocalizer CreateLocalizer()
    {
        var factory = new ResourceManagerStringLocalizerFactory(
            Microsoft.Extensions.Options.Options.Create(new Microsoft.Extensions.Localization.LocalizationOptions { ResourcesPath = "Resources" }),
            Microsoft.Extensions.Logging.Abstractions.NullLoggerFactory.Instance);
        var localizer = new StringLocalizer<SharedErrors>(factory);
        return new ApiMessageLocalizer(localizer);
    }

    [Theory]
    [InlineData("es", "El título del libro es obligatorio.")]
    [InlineData("pl", "Tytuł książki jest wymagany.")]
    public void Localize_TranslatesKnownMessage_ForSupportedCulture(string culture, string expected)
    {
        var originalCulture = CultureInfo.CurrentUICulture;
        try
        {
            CultureInfo.CurrentUICulture = new CultureInfo(culture);
            var localizer = CreateLocalizer();

            // Positive: a known English message is translated for a supported culture.
            var result = localizer.Localize("Book title is required.");

            Assert.Equal(expected, result);
        }
        finally
        {
            CultureInfo.CurrentUICulture = originalCulture;
        }
    }

    [Fact]
    public void Localize_ReturnsOriginalEnglish_ForEnglishCulture()
    {
        var originalCulture = CultureInfo.CurrentUICulture;
        try
        {
            CultureInfo.CurrentUICulture = new CultureInfo("en");
            var localizer = CreateLocalizer();

            var result = localizer.Localize("Book title is required.");

            Assert.Equal("Book title is required.", result);
        }
        finally
        {
            CultureInfo.CurrentUICulture = originalCulture;
        }
    }

    [Fact]
    public void Localize_ReturnsMessageUnchanged_WhenMessageIsNotCatalogued()
    {
        var originalCulture = CultureInfo.CurrentUICulture;
        try
        {
            CultureInfo.CurrentUICulture = new CultureInfo("es");
            var localizer = CreateLocalizer();

            // Negative: an unmapped message (e.g. a corrupted-data exception text
            // not yet catalogued) passes through unchanged rather than throwing
            // or returning an empty/garbled string.
            var result = localizer.Localize("Some future exception message nobody catalogued yet.");

            Assert.Equal("Some future exception message nobody catalogued yet.", result);
        }
        finally
        {
            CultureInfo.CurrentUICulture = originalCulture;
        }
    }

    public static IEnumerable<object[]> KnownMessages() =>
        ApiMessageLocalizer.KnownMessageKeys.Keys.Select(message => new object[] { message });

    [Theory]
    [MemberData(nameof(KnownMessages))]
    public void Localize_HasNonEmptyTranslation_ForEverySupportedCulture(string englishMessage)
    {
        var originalCulture = CultureInfo.CurrentUICulture;
        try
        {
            foreach (var culture in new[] { "es", "pl" })
            {
                CultureInfo.CurrentUICulture = new CultureInfo(culture);
                var localizer = CreateLocalizer();

                // Positive (coverage check): every dictionary entry has a real,
                // non-English translation in both supported non-default cultures,
                // catching drift when a new message is catalogued without resx entries.
                var result = localizer.Localize(englishMessage);

                Assert.False(string.IsNullOrWhiteSpace(result));
                Assert.NotEqual(englishMessage, result);
            }
        }
        finally
        {
            CultureInfo.CurrentUICulture = originalCulture;
        }
    }
}
```

- [ ] **Step 4: Run tests to verify they fail**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~ApiMessageLocalizerTests"`
Expected: FAIL to compile — `ApiMessageLocalizer` does not exist yet.

- [ ] **Step 5: Implement `ApiMessageLocalizer`**

```csharp
// BookWheel/Services/ApiMessageLocalizer.cs
using BookWheel.Resources;
using Microsoft.Extensions.Localization;

namespace BookWheel.Services;

public sealed class ApiMessageLocalizer(IStringLocalizer<SharedErrors> localizer)
{
    private static readonly Dictionary<string, string> KeysByEnglishMessage = new(StringComparer.Ordinal)
    {
        ["Invalid username or password."] = "InvalidCredentials",
        ["The password reset link is invalid or has expired."] = "LinkInvalidOrExpired",
        ["Book title is required."] = "BookTitleRequired",
        ["A valid token and password are required."] = "TokenAndPasswordRequired",
        ["Password reset token data is corrupted and has been quarantined. Restore App_Data from backup."] = "ResetTokenDataCorrupted",
        ["An account already exists."] = "AccountAlreadyExists",
        ["Username and password are required."] = "UsernameAndPasswordRequired",
        ["Create the initial account first."] = "CreateInitialAccountFirst",
        ["Username is required."] = "UsernameRequired",
        ["Username already exists."] = "UsernameAlreadyExists",
        ["User not found."] = "UserNotFound",
        ["At least one administrator account is required."] = "AtLeastOneAdminRequired",
        ["The first account cannot be removed."] = "FirstAccountCannotBeRemoved",
        ["User not found for this reset link."] = "UserNotFoundForResetLink",
        ["Credential data is corrupted and has been quarantined. Restore App_Data from backup."] = "CredentialDataCorrupted",
        ["Book not found."] = "BookNotFound",
        ["No books are available in the wheel."] = "NoBooksAvailable",
        ["Book data is corrupted and has been quarantined. Restore App_Data from backup."] = "BookDataCorrupted",
        ["Administrators can only update other user accounts."] = "AdminsCanOnlyUpdateOthers",
        ["Administrators can only generate reset links for other user accounts."] = "AdminsCanOnlyGenerateResetLinksForOthers",
        ["Administrators can only remove other user accounts."] = "AdminsCanOnlyRemoveOthers",
    };

    public static IReadOnlyDictionary<string, string> KnownMessageKeys => KeysByEnglishMessage;

    public string Localize(string englishMessage)
    {
        if (!KeysByEnglishMessage.TryGetValue(englishMessage, out var key))
        {
            return englishMessage;
        }

        var result = localizer[key];
        return result.ResourceNotFound ? englishMessage : result.Value;
    }
}
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~ApiMessageLocalizerTests"`
Expected: PASS (all cases, including the 21-message coverage `[Theory]`).

- [ ] **Step 7: Commit**

```bash
git add BookWheel/Resources BookWheel/Services/ApiMessageLocalizer.cs BookWheel.Tests/Services/ApiMessageLocalizerTests.cs
git commit -m "Add resx-backed ApiMessageLocalizer for es/pl error messages (#28)"
```

---

## Task 2: Backend — wire localization middleware and controllers

**Files:**
- Modify: `BookWheel/Program.cs`
- Modify: `BookWheel/Controllers/AuthController.cs`
- Modify: `BookWheel/Controllers/BooksController.cs`
- Modify: `BookWheel/Controllers/UsersController.cs`
- Test: `BookWheel.Tests/BookWheelApiTests.cs` (extend)

**Interfaces:**
- Consumes: `ApiMessageLocalizer.Localize(string)` from Task 1.
- Produces: every controller in this task takes `ApiMessageLocalizer` as a constructor parameter alongside its existing dependencies.

- [ ] **Step 1: Register localization services in `Program.cs`**

Add near the other `builder.Services.Add...` calls (after line 29, `AddSingleton<AppMetricsService>()`):

```csharp
builder.Services.AddLocalization();
builder.Services.AddSingleton<ApiMessageLocalizer>();
```

Note: do not set `ResourcesPath = "Resources"` here — `SharedErrors` deliberately lives in the root `BookWheel` namespace (see the comment in `SharedErrors.cs`) precisely so the default (unset) `ResourcesPath` lines up with how the SDK names the embedded `.resx` manifest resources for files under a `Resources/` folder.

`using BookWheel.Services;` is already present; add these two lines to the top of `Program.cs`'s `using` block:
```csharp
using System.Globalization;
using Microsoft.AspNetCore.Localization;
```

After `var app = builder.Build();` (find this line — it's shortly after the `builder.Services...` block), add, before any other `app.Use...` call:

```csharp
var supportedCultures = new[] { "en", "es", "pl" }.Select(c => new CultureInfo(c)).ToList();
app.UseRequestLocalization(new RequestLocalizationOptions
{
    DefaultRequestCulture = new RequestCulture("en"),
    SupportedCultures = supportedCultures,
    SupportedUICultures = supportedCultures
});
```

- [ ] **Step 2: Write the failing integration test**

Add to `BookWheel.Tests/BookWheelApiTests.cs` (find an existing login test for the exact request shape to match — `POST /api/auth/login` with a JSON body of `{ "username": ..., "password": ... }` against a client from `BookWheelWebAppFactory`):

```csharp
[Fact]
public async Task Login_WithBadCredentials_ReturnsSpanishMessage_WhenAcceptLanguageIsSpanish()
{
    using var factory = new BookWheelWebAppFactory();
    using var client = factory.CreateClient();

    var request = new HttpRequestMessage(HttpMethod.Post, "/api/auth/login")
    {
        Content = JsonContent.Create(new { username = "nobody", password = "wrong-password" })
    };
    request.Headers.Add("Accept-Language", "es");

    var response = await client.SendAsync(request);
    var body = await response.Content.ReadFromJsonAsync<JsonElement>();
    var message = body.GetProperty("message").GetString();

    // Positive: the Spanish translation is returned for Accept-Language: es.
    Assert.Equal("Nombre de usuario o contraseña incorrectos.", message);

    // Negative: the raw English string is not leaking through for a non-English request.
    Assert.NotEqual("Invalid username or password.", message);
}
```

- [ ] **Step 3: Run test to verify it fails**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Login_WithBadCredentials_ReturnsSpanishMessage"`
Expected: FAIL — message is still the hardcoded English string (`AuthController` doesn't localize yet).

- [ ] **Step 4: Wire `ApiMessageLocalizer` into the controllers**

In `BookWheel/Controllers/AuthController.cs`: add `private readonly ApiMessageLocalizer _errors;` and take `ApiMessageLocalizer errors` in the constructor, assigning `_errors = errors;`. Add `using BookWheel.Services;` if not already present. Change line 140 from:
```csharp
return Unauthorized(new { message = "Invalid username or password." });
```
to:
```csharp
return Unauthorized(new { message = _errors.Localize("Invalid username or password.") });
```
Change line 205 from `return BadRequest(new { message = ex.Message });` to `return BadRequest(new { message = _errors.Localize(ex.Message) });`. Change line 215 similarly, wrapping `"The password reset link is invalid or has expired."` in `_errors.Localize(...)`.

In `BookWheel/Controllers/BooksController.cs`: add the same constructor parameter/field pattern. Wrap both `"Book title is required."` literals (lines 58, 83) and the `ex.Message` on line 127 with `_errors.Localize(...)`.

In `BookWheel/Controllers/UsersController.cs`: add the same constructor parameter/field pattern. Wrap all four `ex.Message` occurrences (lines 84, 141, 182, 225) and the three literal messages (lines 104, 161, 202) with `_errors.Localize(...)`.

- [ ] **Step 5: Run test to verify it passes**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Login_WithBadCredentials_ReturnsSpanishMessage"`
Expected: PASS.

- [ ] **Step 6: Run the full backend test suite to check for regressions**

Run: `dotnet test BookWheel.slnx`
Expected: PASS — no existing test asserts on the literal English error strings changing shape (they still return `{ message: "..." }` in English for `en`/unset `Accept-Language`).

- [ ] **Step 7: Commit**

```bash
git add BookWheel/Program.cs BookWheel/Controllers/AuthController.cs BookWheel/Controllers/BooksController.cs BookWheel/Controllers/UsersController.cs BookWheel.Tests/BookWheelApiTests.cs
git commit -m "Localize API error messages via Accept-Language (#28)"
```

---

## Task 3: Frontend — `i18n.js` catalog and core functions

**Files:**
- Create: `BookWheel/wwwroot/js/i18n.js`
- Test: `BookWheel.Tests/BookWheelFrontendTests.cs` (extend)

**Interfaces:**
- Produces: global `window.BookWheelI18n` object with `{ t(key, params), setLocale(locale), getCurrentLocale(), SUPPORTED_LOCALES, LANGUAGE_NAMES }`, used by `app.js` and `index.html` inline init in later tasks.
- Produces: `data-i18n`, `data-i18n-placeholder`, `data-i18n-aria-label`, `data-i18n-title` as the attribute names `applyStaticTranslations` looks for.

- [ ] **Step 1: Write the file**

```javascript
// BookWheel/wwwroot/js/i18n.js
(function () {
  const SUPPORTED_LOCALES = ['en', 'es', 'pl'];
  const DEFAULT_LOCALE = 'en';
  const LOCALE_STORAGE_KEY = 'bookwheel-locale';
  const LANGUAGE_NAMES = { en: 'English', es: 'Español', pl: 'Polski' };

  const TRANSLATIONS = {
    en: { /* ...full English catalog from Appendix B, nested by namespace... */ },
    es: { /* ...full Spanish catalog from Appendix B... */ },
    pl: { /* ...full Polish catalog from Appendix B... */ }
  };

  function detectInitialLocale() {
    const stored = localStorage.getItem(LOCALE_STORAGE_KEY);
    if (SUPPORTED_LOCALES.includes(stored)) {
      return stored;
    }

    const browserLocales = navigator.languages && navigator.languages.length
      ? navigator.languages
      : [navigator.language];

    for (const browserLocale of browserLocales) {
      const shortCode = (browserLocale || '').slice(0, 2).toLowerCase();
      if (SUPPORTED_LOCALES.includes(shortCode)) {
        return shortCode;
      }
    }

    return DEFAULT_LOCALE;
  }

  let currentLocale = detectInitialLocale();

  function resolveCatalogValue(locale, key) {
    const segments = key.split('.');
    let node = TRANSLATIONS[locale];
    for (const segment of segments) {
      if (!node || typeof node !== 'object') {
        return undefined;
      }
      node = node[segment];
    }
    return typeof node === 'string' ? node : undefined;
  }

  function interpolate(template, params) {
    if (!params) {
      return template;
    }
    return template.replace(/\{(\w+)\}/g, (match, token) =>
      Object.prototype.hasOwnProperty.call(params, token) ? String(params[token]) : match
    );
  }

  function t(key, params) {
    const value = resolveCatalogValue(currentLocale, key)
      ?? resolveCatalogValue(DEFAULT_LOCALE, key)
      ?? key;
    return interpolate(value, params);
  }

  function applyStaticTranslations(root) {
    const scope = root || document;

    scope.querySelectorAll('[data-i18n]').forEach(element => {
      element.textContent = t(element.getAttribute('data-i18n'));
    });
    scope.querySelectorAll('[data-i18n-placeholder]').forEach(element => {
      element.setAttribute('placeholder', t(element.getAttribute('data-i18n-placeholder')));
    });
    scope.querySelectorAll('[data-i18n-aria-label]').forEach(element => {
      element.setAttribute('aria-label', t(element.getAttribute('data-i18n-aria-label')));
    });
    scope.querySelectorAll('[data-i18n-title]').forEach(element => {
      element.setAttribute('title', t(element.getAttribute('data-i18n-title')));
    });
  }

  function getCurrentLocale() {
    return currentLocale;
  }

  function setLocale(locale) {
    if (!SUPPORTED_LOCALES.includes(locale)) {
      return;
    }

    currentLocale = locale;
    localStorage.setItem(LOCALE_STORAGE_KEY, locale);
    document.documentElement.setAttribute('lang', locale);
    applyStaticTranslations(document);
    window.dispatchEvent(new CustomEvent('bookwheel:locale-changed', { detail: { locale } }));
  }

  document.addEventListener('DOMContentLoaded', () => {
    document.documentElement.setAttribute('lang', currentLocale);
    applyStaticTranslations(document);
  });

  window.BookWheelI18n = {
    t,
    setLocale,
    getCurrentLocale,
    applyStaticTranslations,
    SUPPORTED_LOCALES,
    LANGUAGE_NAMES
  };
})();
```

Fill in the three `TRANSLATIONS.en/es/pl` object literals with every key/value from Appendix B, nested by namespace exactly as the table groups are named (`common`, `app`, `auth`, `theme`, `lang`, `wheel`, `books`, `users`, `transfer`). This is data entry, not logic — copy each table row into `key: 'value'` pairs (escaping embedded `"` as `\"` where the table shows literal quote marks, e.g. `removeConfirmMessage`).

- [ ] **Step 2: Write the failing test**

Add to `BookWheel.Tests/BookWheelFrontendTests.cs`:

```csharp
[Fact]
public async Task Frontend_Should_Serve_I18n_Script_With_All_Locale_Catalogs()
{
    using var factory = new BookWheelWebAppFactory();
    using var client = factory.CreateClient();

    var response = await client.GetAsync("/js/i18n.js");
    Assert.Equal(HttpStatusCode.OK, response.StatusCode);

    var script = await response.Content.ReadAsStringAsync();

    // Positive: the catalog structure, all three locales, and representative
    // translated strings for each supported language are present.
    Assert.Contains("SUPPORTED_LOCALES = ['en', 'es', 'pl']", script, StringComparison.Ordinal);
    Assert.Contains("BookWheelI18n", script, StringComparison.Ordinal);
    Assert.Contains("Iniciar sesión", script, StringComparison.Ordinal);
    Assert.Contains("Zaloguj się", script, StringComparison.Ordinal);
    Assert.Contains("Book title is required.", script, StringComparison.Ordinal);

    // Negative: the old raw-English-only pagination string no longer lives in
    // app.js (it moved into the catalog as a keyed, per-locale entry) — this
    // file, not app.js, is now the single source of truth for that text.
    Assert.DoesNotContain("const wheelBooks = [placeholder]", script, StringComparison.Ordinal);
}
```

- [ ] **Step 3: Run test to verify it fails**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Frontend_Should_Serve_I18n_Script_With_All_Locale_Catalogs"`
Expected: FAIL with 404 — `/js/i18n.js` doesn't exist yet.

- [ ] **Step 4: Run test to verify it passes**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Frontend_Should_Serve_I18n_Script_With_All_Locale_Catalogs"`
Expected: PASS once Step 1's file is in place with the full catalog.

- [ ] **Step 5: Commit**

```bash
git add BookWheel/wwwroot/js/i18n.js BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Add i18n.js locale catalog and translation helpers (#28)"
```

---

## Task 4: Frontend — `index.html` markup (data-i18n attributes + language toggle)

**Files:**
- Modify: `BookWheel/wwwroot/index.html`
- Test: `BookWheel.Tests/BookWheelFrontendTests.cs` (extend)

**Interfaces:**
- Consumes: translation keys from Appendix B (must match exactly what Task 3's catalog defines).
- Produces: a `langToggleBtn` button (id) and `langToggleLabel` span (id) for Task 9 to wire up click behavior.

- [ ] **Step 1: Write the failing test**

```csharp
[Fact]
public async Task Home_Page_Should_Include_I18n_Attributes_And_Language_Toggle()
{
    using var factory = new BookWheelWebAppFactory();
    using var client = factory.CreateClient();

    var response = await client.GetAsync("/");
    var html = await response.Content.ReadAsStringAsync();

    // Positive: language toggle exists and static text is externalized via data-i18n.
    Assert.Contains("id=\"langToggleBtn\"", html, StringComparison.Ordinal);
    Assert.Contains("id=\"langToggleLabel\"", html, StringComparison.Ordinal);
    Assert.Contains("data-i18n=\"auth.loginSubmit\"", html, StringComparison.Ordinal);
    Assert.Contains("data-i18n=\"books.heading\"", html, StringComparison.Ordinal);
    Assert.Contains("src=\"js/i18n.js", html, StringComparison.Ordinal);

    // Negative: the i18n script must load before app.js, since app.js calls
    // BookWheelI18n at init time.
    var i18nIndex = html.IndexOf("src=\"js/i18n.js", StringComparison.Ordinal);
    var appJsIndex = html.IndexOf("src=\"js/app.js", StringComparison.Ordinal);
    Assert.True(i18nIndex >= 0 && appJsIndex >= 0 && i18nIndex < appJsIndex);
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Home_Page_Should_Include_I18n_Attributes_And_Language_Toggle"`
Expected: FAIL — none of this markup exists yet.

- [ ] **Step 3: Add the `<script>` tag**

In `BookWheel/wwwroot/index.html`, immediately before the existing `<script src="js/app.js?v=__ASSET_VERSION__"></script>` line, add:
```html
<script src="js/i18n.js?v=__ASSET_VERSION__"></script>
```

- [ ] **Step 4: Add the language toggle button**

Immediately after the opening `<body>` tag and before the existing `<a class="skip-link" ...>` element, add a persistent toggle (visible pre- and post-login):
```html
<div class="lang-toggle-wrap">
  <button id="langToggleBtn" type="button" class="secondary icon-btn" title="Change language" aria-label="Change language, current: English">
    <span id="langToggleLabel" class="icon-glyph" aria-hidden="true">EN</span>
  </button>
</div>
```

- [ ] **Step 5: Add `data-i18n` attributes to static text**

Apply these substitutions (attribute added, existing text/attribute value left as the English fallback so the page still reads correctly before `i18n.js` runs):

| Element | Attribute to add |
|---|---|
| `<a class="skip-link">` | `data-i18n="common.skipToContent"` |
| `#authTitle` | `data-i18n="auth.loginTitle"` |
| `Username` label text node (login form) | wrap in `<span data-i18n="auth.usernameLabel">Username</span>` |
| `Password` label text node | wrap in `<span data-i18n="auth.passwordLabel">Password</span>` |
| `#authSubmitBtn` | `data-i18n="auth.loginSubmit"` |
| `New password` label text | wrap in `<span data-i18n="auth.newPasswordLabel">New password</span>` |
| `Confirm password` label text | wrap in `<span data-i18n="auth.confirmPasswordLabel">Confirm password</span>` |
| `#resetPasswordSubmitBtn` | `data-i18n="auth.setPasswordSubmit"` |
| `<h1>Book Wheel</h1>` (toolbar) | `data-i18n="app.title"` |
| `#userManagementBtn` | `data-i18n="users.manageUsersBtn"` |
| `#importExportBtn` | `data-i18n-title="transfer.importExportLabel" data-i18n-aria-label="transfer.importExportLabel"` |
| `#themeToggleBtn` | `data-i18n-title="theme.switchThemeLabel" data-i18n-aria-label="theme.switchThemeLabel"` |
| `#logoutBtn` | `data-i18n="auth.logoutBtn"` |
| `<h2>Spin wheel</h2>` | `data-i18n="wheel.heading"` |
| `#wheelCanvas` | `data-i18n-aria-label="wheel.canvasAriaLabel"` |
| `#spinBtn` | `data-i18n="wheel.spinBtn"` |
| `<h2>Active books</h2>` | `data-i18n="books.heading"` |
| `bookTitle` sr-only label | `data-i18n="books.titleLabel"` |
| `#bookTitle` input | `data-i18n-placeholder="books.titlePlaceholder"` |
| `#bookForm button[type=submit]` | `data-i18n="books.addBtn"` |
| `#booksPrevPageBtn` | `data-i18n="books.prevPage"` |
| `#booksNextPageBtn` | `data-i18n="books.nextPage"` |
| `#editDialog h2` | `data-i18n="books.editDialogTitle"` |
| `#editDialog` title label text | `data-i18n="books.titleLabel"` |
| `#cancelEditBtn` | `data-i18n="common.cancel"` |
| `#editForm button[type=submit]` | `data-i18n="common.save"` |
| `#deleteDialog h2` | `data-i18n="books.deleteDialogTitle"` |
| `#cancelDeleteBtn` | `data-i18n="common.cancel"` |
| `#confirmDeleteBtn` | `data-i18n="common.remove"` |
| `#deleteUserDialog h2` | `data-i18n="users.deleteDialogTitle"` |
| static paragraph "This action permanently removes..." | `data-i18n="users.deleteWarning"` |
| `#cancelDeleteUserBtn` | `data-i18n="common.cancel"` |
| `#confirmDeleteUserBtn` | `data-i18n="users.removeUserBtn"` |
| `#resetLinkDialog h2` | `data-i18n="users.resetLinkDialogTitle"` |
| "Share this link with the user" label text | `data-i18n="users.resetLinkShareLabel"` |
| `#copyResetLinkBtn` | `data-i18n="users.copyLinkBtn"` |
| `#closeResetLinkBtn` | `data-i18n="common.close"` |
| `#transferDialog h2` | `data-i18n="transfer.dialogTitle"` |
| `#importTabBtn` | `data-i18n="transfer.importTab"` |
| `#exportTabBtn` | `data-i18n="transfer.exportTab"` |
| import panel description paragraph | `data-i18n="transfer.importDescription"` |
| "Choose JSON file" label text | `data-i18n="transfer.chooseFileLabel"` |
| `#cancelTransferBtn` | `data-i18n="common.close"` |
| `#importFileBtn` | `data-i18n="transfer.importFileBtn"` |
| export panel description paragraph | `data-i18n="transfer.exportDescription"` |
| `#closeExportBtn` | `data-i18n="common.close"` |
| `#downloadExportBtn` | `data-i18n="transfer.downloadBtn"` |
| `#userManagementDialog h2` | `data-i18n="users.managementDialogTitle"` |
| "Admin workspace" kicker span | `data-i18n="users.adminWorkspaceKicker"` |
| user management intro paragraph | `data-i18n="users.managementDescription"` |
| "Create user" h3 | `data-i18n="users.createUserHeading"` |
| "New account" tag span | `data-i18n="users.newAccountTag"` |
| create-user description paragraph | `data-i18n="users.createUserDescription"` |
| `createUserUsername` label text | `data-i18n="auth.usernameLabel"` |
| Administrator checkbox label text | `data-i18n="users.administratorLabel"` |
| create user submit button | `data-i18n="users.createUserBtn"` |
| reset button | `data-i18n="common.clear"` |
| "Existing users" h3 | `data-i18n="users.existingUsersHeading"` |
| "Directory" tag span | `data-i18n="users.directoryTag"` |
| update-description paragraph | `data-i18n="users.updateDescription"` |
| `userSearchInput` label text | `data-i18n="users.searchLabel"` |
| `#userSearchInput` | `data-i18n-placeholder="users.searchPlaceholder"` |
| `#closeUserManagementBtn` | `data-i18n="common.close"` |

(This is a mechanical find-and-wrap pass — each row's element already exists in `index.html`; add the attribute to the existing tag, or wrap bare label text in a `<span data-i18n="...">` where the label has no dedicated child element to attach the attribute to, matching the pattern of e.g. `<label>` `Username` `<input ...>` today.)

- [ ] **Step 6: Run test to verify it passes**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Home_Page_Should_Include_I18n_Attributes_And_Language_Toggle"`
Expected: PASS.

- [ ] **Step 7: Run the existing `Home_Page_Should_Render_Main_UI_Structure` test to check for regressions**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~Home_Page_Should_Render_Main_UI_Structure"`
Expected: PASS — all asserted `id="..."` attributes are untouched; only new `data-i18n*` attributes were added alongside them.

- [ ] **Step 8: Commit**

```bash
git add BookWheel/wwwroot/index.html BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Add data-i18n attributes and language toggle markup to index.html (#28)"
```

---

## Task 5: Frontend — `app.js` Accept-Language header + auth/login section

**Files:**
- Modify: `BookWheel/wwwroot/js/app.js`
- Modify: `BookWheel.Tests/BookWheelFrontendTests.cs` (relocate 1 assertion)

**Interfaces:**
- Consumes: `window.BookWheelI18n.t(key, params)` and `.getCurrentLocale()` from Task 3.

- [ ] **Step 1: Add `Accept-Language` to the shared fetch wrapper**

In `requestJson` (around line 410-428), change:
```javascript
async function requestJson(url, options = {}) {
  const response = await fetch(url, {
    headers: {
      'Content-Type': 'application/json',
      ...(options.headers || {})
    },
    credentials: 'same-origin',
    ...options
  });
```
to:
```javascript
async function requestJson(url, options = {}) {
  const response = await fetch(url, {
    headers: {
      'Content-Type': 'application/json',
      'Accept-Language': window.BookWheelI18n.getCurrentLocale(),
      ...(options.headers || {})
    },
    credentials: 'same-origin',
    ...options
  });
```
Also change the fallback message on line 424 from `payload?.message || 'Request failed.'` to `payload?.message || window.BookWheelI18n.t('common.requestFailed')`.

- [ ] **Step 2: Replace auth/login section strings**

Replace every hardcoded string in `setAuthMode` (lines 364-388) with `t()` calls, e.g.:
```javascript
function setAuthMode(mode) {
  authMode = mode;
  const { t } = window.BookWheelI18n;

  if (resetTokenFromUrl) {
    authTitle.textContent = t('auth.setPasswordTitle');
    authMessage.textContent = t('auth.setPasswordSubtitle');
    loginForm.classList.add('hidden');
    resetPasswordForm.classList.remove('hidden');
    return;
  }

  loginForm.classList.remove('hidden');
  resetPasswordForm.classList.add('hidden');

  if (mode === 'setup') {
    authTitle.textContent = t('auth.setupTitle');
    authMessage.textContent = t('auth.setupSubtitle');
    authSubmitBtn.textContent = t('auth.setupSubmit');
    return;
  }

  authTitle.textContent = t('auth.loginTitle');
  authMessage.textContent = t('auth.loginSubtitle');
  authSubmitBtn.textContent = t('auth.loginSubmit');
}
```

Apply the same `t()`-call substitution pattern (using the exact keys from Appendix B's `auth` table) to: the `loginForm` submit handler (lines 1210-1256 — `credentialsRequiredError`, `creatingAccountBusy`/`loggingInBusy` busy text, `accountCreatedToast`/`signedInToast`, `connectionError` fallback for `'Failed to fetch'`); the `logoutBtn` handler (line 1391, `signedOutToast`); the `resetPasswordForm` submit handler (lines 1481-1548 — `resetTokenMissingError`, `bothPasswordsRequiredError`, `passwordTooShortError`, `passwordsMismatchError`, `common.saving`, `passwordUpdatedMessage`, `passwordUpdatedToast`); and the bootstrap IIFE's reset-token validation branch (lines 1564-1604 — `validatingLinkTitle`, `validatingLinkSubtitle`, `setPasswordForUser`/`defaultAccountName` composed as `t('auth.setPasswordForUser', { username: validation.username || t('auth.defaultAccountName') })`, `linkExpiresAt`/`linkExpires24h`, `linkValidatedToast`, fallback `linkInvalidError`).

- [ ] **Step 3: Relocate the broken existing assertion**

`BookWheelFrontendTests.cs`'s `Frontend_Script_Should_Contain_Pagination_And_Selection_Behavior` test asserts `Assert.Contains("Create your Book Wheel account", script, ...)` against `/js/app.js` — that literal string no longer appears in `app.js` after Step 2 (it's now `t('auth.setupTitle')`, with the English text living in `i18n.js`). Remove that one assertion line from the `app.js` test and add it instead to `Frontend_Should_Serve_I18n_Script_With_All_Locale_Catalogs` (Task 3's test) as:
```csharp
Assert.Contains("Create your Book Wheel account", script, StringComparison.Ordinal);
```
(added to that test's list of positive assertions, checking `/js/i18n.js` content instead of `/js/app.js`).

- [ ] **Step 4: Run the full frontend test suite**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~BookWheelFrontendTests"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add BookWheel/wwwroot/js/app.js BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Localize auth/login flow strings and send Accept-Language header (#28)"
```

---

## Task 6: Frontend — `app.js` wheel/spin section

**Files:**
- Modify: `BookWheel/wwwroot/js/app.js`

**Interfaces:**
- Consumes: `window.BookWheelI18n.t(key, params)`.

- [ ] **Step 1: Replace wheel/spin strings**

Using the `wheel` namespace keys from Appendix B, replace: `renderWheelAccessibilitySummary` (lines 207-225 — `emptyStatus`, `summaryCount` with `{ count: wheelBooks.length }`); `drawWheel`'s canvas empty-state text (line 788, `addBooksPrompt`); the `spinBtn` click handler (lines 1307-1358 — `spinningBusy` busy text, `selectedBookEl` busy text also `spinningBusy`, thrown-error strings `addBooksError`/`selectionNotFoundError`, `lastSelected` with `{ title: selected.title }`, idle text `spinBtn` key, toast `selectedToast` with `{ title: selected.title }`); and `renderActiveBooks`'s empty-state `selectedBookEl.textContent` (line 826, `emptyPrompt`).

Example for the spin handler's busy/idle text:
```javascript
spinBtn.addEventListener('click', async () => {
  if (spinning || !activeBooks.length) {
    return;
  }
  const { t } = window.BookWheelI18n;

  spinning = true;
  spinBtn.disabled = true;
  spinBtn.textContent = t('wheel.spinningBusy');
  selectedBookEl.textContent = t('wheel.spinningBusy');

  try {
    const result = await requestJson('/api/books/spin', { method: 'POST' });
    const selected = result.selected;

    if (!wheelBooks.length) {
      throw new Error(t('wheel.addBooksError'));
    }

    const selectedIndex = wheelBooks.findIndex(book => book.id === selected.id);

    if (selectedIndex < 0) {
      throw new Error(t('wheel.selectionNotFoundError'));
    }
    // ...rotation math unchanged...

    setTimeout(async () => {
      activeBooks = result.activeBooks || [];
      clampCurrentPage();
      drawWheel();
      renderActiveBooks();
      selectedBookEl.textContent = t('wheel.lastSelected', { title: selected.title });
      spinning = false;
      spinBtn.disabled = activeBooks.length === 0;
      spinBtn.textContent = t('wheel.spinBtn');
      showToast(t('wheel.selectedToast', { title: selected.title }), 'success');
    }, spinDelayMs);
  } catch (error) {
    spinning = false;
    selectedBookEl.textContent = error.message;
    spinBtn.textContent = t('wheel.spinBtn');
    showToast(error.message, 'error');
    await refreshBooks();
  }
});
```

- [ ] **Step 2: Run the frontend test suite**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~BookWheelFrontendTests"`
Expected: PASS (no existing assertion checks the exact wheel-strings' literal text in `app.js` except `"Last selected:"` — remove that one assertion from `Frontend_Script_Should_Contain_Pagination_And_Selection_Behavior` and add `Assert.Contains("Last selected:", script, ...)` to the i18n.js test in Task 3 instead, same relocation pattern as Task 5 Step 3).

- [ ] **Step 3: Commit**

```bash
git add BookWheel/wwwroot/js/app.js BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Localize spin wheel strings (#28)"
```

---

## Task 7: Frontend — `app.js` book list, pagination, and edit/delete dialogs

**Files:**
- Modify: `BookWheel/wwwroot/js/app.js`

**Interfaces:**
- Consumes: `window.BookWheelI18n.t(key, params)`.

- [ ] **Step 1: Replace pagination and count strings**

`renderPagination` (line 324, `books.pageInfo` with `{ current: currentPage, total: totalPages }`). `renderBookCount` (lines 329-334): pick `books.countOne` when `totalBooks === 1` else `books.countOther` with `{ count: totalBooks }`, then compose with `books.pageInfo`:
```javascript
function renderBookCount() {
  const { t } = window.BookWheelI18n;
  const totalBooks = activeBooks.length;
  const totalPages = getTotalPages();
  const bookLabel = totalBooks === 1 ? t('books.countOne') : t('books.countOther', { count: totalBooks });
  const pageInfo = t('books.pageInfo', { current: currentPage, total: totalPages });
  booksTotalCountEl.textContent = `${bookLabel} • ${pageInfo}`;
}
```

- [ ] **Step 2: Replace book row, empty-state, and edit/delete dialog strings**

`renderActiveBooks` (lines 817-866 — empty state `books.emptyHeading`/`emptyHint1`/`emptyHint2`, book row `titleButton.title` = `books.editTitleHint`, `aria-label` = `t('books.editAria', { title: book.title })`, `removeButton.textContent` = `t('common.remove')`, `.title` = `books.removeHint`, `aria-label` = `t('books.removeAria', { title: book.title })`). `saveEdit` (lines 889-908 — `books.titleEmptyError`, `books.updatedMessage`, `books.updatedToast`). `removeBook` (line 913, `t('books.removeConfirmMessage', { title: book.title })`). `confirmDelete` (lines 939-940, `books.removedMessage`/`removedToast`). `bookForm` submit handler (lines 1258-1287, `books.titleRequiredError`, `books.addedMessage`, `books.addedToast`).

- [ ] **Step 3: Run the frontend test suite, relocating any broken literal-text assertions**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~BookWheelFrontendTests"`. The existing `Frontend_Script_Should_Contain_Pagination_And_Selection_Behavior` test's `Assert.Contains("Page ${currentPage} of ${totalPages}", ...)` and `Assert.Contains("Book title is required.", ...)` assertions will fail since those literals moved to `i18n.js`. Remove them from that test and add equivalents to Task 3's i18n.js test:
```csharp
Assert.Contains("Page {current} of {total}", script, StringComparison.Ordinal);
Assert.Contains("Book title is required.", script, StringComparison.Ordinal);
```
Expected after the move: PASS.

- [ ] **Step 4: Commit**

```bash
git add BookWheel/wwwroot/js/app.js BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Localize book list, pagination, and edit/delete dialog strings (#28)"
```

---

## Task 8: Frontend — `app.js` user management section

**Files:**
- Modify: `BookWheel/wwwroot/js/app.js`

**Interfaces:**
- Consumes: `window.BookWheelI18n.t(key, params)`.

- [ ] **Step 1: Replace user-management strings**

Using the `users` namespace keys from Appendix B, replace strings across: `applyCurrentUser` (line 348, `users.greeting` with `{ username }`); `renderUserCount` (lines 452-465, `zeroUsers`/`totalUsers`/`filteredUsers`); `openDeleteUserDialog` (line 490, `t('users.deleteConfirmMessage', { username: user.username })`); `confirmDeleteUser` (lines 508-509, `removedUserMessage`/`removedUserToast`); `renderUserRows` (lines 513-756 — `noMatchFilter`, `roleAdmin`/`roleStandard`, `createdOn`/`createdUnavailable`, `auth.usernameLabel`, `adminCheckbox`, `disabledCheckbox`, `requireResetCheckbox`, `lockedCheckbox`, `common.save`/`common.remove`, `saveChangesAria`, `removeUserAria`, `generateResetLinkBtn`, `generateResetLinkAria`, the three `.title` hint strings `ownAccountHint`/`cannotResetOwnHint`/`cannotRemoveOwnHint`/`cannotRemoveFirstHint`, `common.saving` for `togglePendingState`, `currentUserSuffix`, `usernameRequiredError`, `updatedUserMessage`, `common.generating`, `generatedResetLinkMessage`/`generatedResetLinkToast`); `openResetLinkDialog` (lines 390-401, `linkExpiresAt`/`linkExpires24h`, `resetLinkCreated`); `createUserForm` submit handler (lines 1409-1456, `usernameRequiredError`, `common.creating`/`users.createUserBtn` busy text, `createdUserMessage`/`createdUserToast`); `copyResetLinkBtn` handler (lines 1458-1473, `noResetLinkError`, `linkCopiedMessage`, `copyFailedMessage`).

Example for `renderUserCount`:
```javascript
function renderUserCount(filteredCount, totalCount) {
  if (!userCountBadge) {
    return;
  }
  const { t } = window.BookWheelI18n;

  if (totalCount === 0) {
    userCountBadge.textContent = t('users.zeroUsers');
    return;
  }

  userCountBadge.textContent = filteredCount === totalCount
    ? t('users.totalUsers', { count: totalCount })
    : t('users.filteredUsers', { filtered: filteredCount, total: totalCount });
}
```

- [ ] **Step 2: Run the frontend test suite, relocating any broken literal-text assertions**

Run: `dotnet test BookWheel.slnx --filter "FullyQualifiedName~BookWheelFrontendTests"`. `Frontend_Script_Should_Contain_Pagination_And_Selection_Behavior`'s `Assert.Contains("Generate reset link", ...)` will fail. Remove it and add to the i18n.js test:
```csharp
Assert.Contains("Generate reset link", script, StringComparison.Ordinal);
```
Expected after the move: PASS.

- [ ] **Step 3: Commit**

```bash
git add BookWheel/wwwroot/js/app.js BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Localize user management strings (#28)"
```

---

## Task 9: Frontend — `app.js` transfer (import/export) dialog and theme/language toggle labels

**Files:**
- Modify: `BookWheel/wwwroot/js/app.js`

**Interfaces:**
- Consumes: `window.BookWheelI18n.t(key, params)`, `.setLocale(locale)`, `.getCurrentLocale()`, `.SUPPORTED_LOCALES`, `.LANGUAGE_NAMES`.
- Produces: click handler wired to `#langToggleBtn` (the button Task 4 added to `index.html`).

- [ ] **Step 1: Replace transfer dialog strings**

Using the `transfer` namespace keys from Appendix B, replace strings in: `parseImportTitles` (line 996, `invalidJsonError`); `importBooksFromJsonFile` (lines 1017-1072, `chooseFileError`, `emptyFileError`, `noTitlesError`, `t('transfer.importCompleteMessage', { added: addedCount, skipped: skippedMatches })`); `downloadExportJsonFile` (line 1091, `exportStartedMessage`).

- [ ] **Step 2: Replace theme toggle labels**

Replace `THEME_LABELS` (line 102) with keys instead of hardcoded English:
```javascript
const THEME_LABEL_KEYS = { [DARK_THEME]: 'theme.dark', [LIGHT_THEME]: 'theme.light', [HIGH_CONTRAST_THEME]: 'theme.highContrast' };
```
In `applyTheme` (lines 286-302), change:
```javascript
const nextThemeLabel = THEME_LABELS[nextTheme];
themeToggleBtn.setAttribute('aria-label', `Switch to ${nextThemeLabel}`);
themeToggleBtn.setAttribute('title', `Switch to ${nextThemeLabel}`);
```
to:
```javascript
const { t } = window.BookWheelI18n;
const nextThemeLabel = t(THEME_LABEL_KEYS[nextTheme]);
themeToggleBtn.setAttribute('aria-label', t('theme.switchTo', { label: nextThemeLabel }));
themeToggleBtn.setAttribute('title', t('theme.switchTo', { label: nextThemeLabel }));
```

- [ ] **Step 3: Wire the language toggle button**

Add near the theme toggle wiring (after line 1556-1558's `themeToggleBtn` listener):
```javascript
const langToggleBtn = document.getElementById('langToggleBtn');
const langToggleLabel = document.getElementById('langToggleLabel');

function applyLanguageToggleLabel() {
  if (!langToggleBtn || !langToggleLabel) {
    return;
  }
  const { getCurrentLocale, LANGUAGE_NAMES, t } = window.BookWheelI18n;
  const locale = getCurrentLocale();
  langToggleLabel.textContent = locale.toUpperCase();
  langToggleBtn.setAttribute('aria-label', t('lang.switchLanguageAria', { language: LANGUAGE_NAMES[locale] }));
  langToggleBtn.setAttribute('title', t('lang.switchLanguageAria', { language: LANGUAGE_NAMES[locale] }));
}

if (langToggleBtn) {
  langToggleBtn.addEventListener('click', () => {
    const { SUPPORTED_LOCALES, getCurrentLocale, setLocale } = window.BookWheelI18n;
    const currentIndex = SUPPORTED_LOCALES.indexOf(getCurrentLocale());
    const nextLocale = SUPPORTED_LOCALES[(currentIndex + 1) % SUPPORTED_LOCALES.length];
    setLocale(nextLocale);
  });
}

window.addEventListener('bookwheel:locale-changed', () => {
  applyLanguageToggleLabel();
  applyTheme(document.documentElement.getAttribute('data-theme') || DARK_THEME);
  renderWheelAccessibilitySummary();
  if (!appView.classList.contains('hidden')) {
    renderActiveBooks();
  }
  if (!userManagementDialog.classList.contains('hidden') && userManagementDialog.hasAttribute('open')) {
    renderUserRows(allUsers);
  }
});

applyLanguageToggleLabel();
```
This re-renders already-rendered dynamic text (book list, wheel summary, theme toggle aria-label, open user list) immediately when the language changes, per the design doc's data-flow step 2.

- [ ] **Step 4: Run the full frontend and backend test suites**

Run: `dotnet test BookWheel.slnx`
Expected: PASS. `Frontend_Script_Should_Contain_Theme_Toggle_Behavior` still passes since `THEME_STORAGE_KEY`/`themeToggleBtn`/`themeToggleIcon`/`data-theme` literals are untouched — only the label text moved to `t()`.

- [ ] **Step 5: Commit**

```bash
git add BookWheel/wwwroot/js/app.js
git commit -m "Localize transfer dialog and theme labels; wire language toggle button (#28)"
```

---

## Task 10: Frontend — new i18n-specific tests and cleanup pass

**Files:**
- Modify: `BookWheel.Tests/BookWheelFrontendTests.cs`

**Interfaces:**
- None new — this task only adds/tightens test coverage over Tasks 3-9's output.

- [ ] **Step 1: Add a negative test for the language toggle's initial state**

```csharp
[Fact]
public async Task Home_Page_Language_Toggle_Should_Not_Ship_Untranslated_Placeholder_Text()
{
    using var factory = new BookWheelWebAppFactory();
    using var client = factory.CreateClient();

    var response = await client.GetAsync("/");
    var html = await response.Content.ReadAsStringAsync();

    var toggleStart = html.IndexOf("id=\"langToggleBtn\"", StringComparison.Ordinal);
    var toggleSnippet = html.Substring(toggleStart, Math.Min(400, html.Length - toggleStart));

    // Positive: the toggle's static label is a short language code, not a full sentence.
    Assert.Contains(">EN<", toggleSnippet, StringComparison.Ordinal);

    // Negative: no leftover "TODO"/placeholder marker text made it into the toggle markup.
    Assert.DoesNotContain("TODO", toggleSnippet, StringComparison.OrdinalIgnoreCase);
}
```

- [ ] **Step 2: Add a positive test asserting locale-change re-render wiring exists**

```csharp
[Fact]
public async Task Frontend_Script_Should_Re_Render_Dynamic_Content_On_Locale_Change()
{
    using var factory = new BookWheelWebAppFactory();
    using var client = factory.CreateClient();

    var response = await client.GetAsync("/js/app.js");
    var script = await response.Content.ReadAsStringAsync();

    // Positive: dynamic UI re-renders (not just a page reload) when the
    // language changes, so an in-progress session doesn't show mixed languages.
    Assert.Contains("bookwheel:locale-changed", script, StringComparison.Ordinal);
    Assert.Contains("langToggleBtn", script, StringComparison.Ordinal);

    // Negative: the language toggle must not require navigator.language to be
    // re-read on every click — it should cycle explicitly through SUPPORTED_LOCALES,
    // so the user's manual choice isn't overridden by browser settings mid-session.
    Assert.DoesNotContain("langToggleBtn.addEventListener('click', () => setLocale(navigator.language", script, StringComparison.Ordinal);
}
```

- [ ] **Step 3: Run the full test suite**

Run: `dotnet test BookWheel.slnx`
Expected: PASS — all tests, old and new.

- [ ] **Step 4: Commit**

```bash
git add BookWheel.Tests/BookWheelFrontendTests.cs
git commit -m "Add i18n toggle and locale-change re-render test coverage (#28)"
```

---

## Task 11: Documentation

**Files:**
- Modify: `README.md`
- Modify: `IMPROVEMENT_ROADMAP.md`

- [ ] **Step 1: Add a README "Internationalization" subsection**

Add a new `## Internationalization` section after the existing `## Features` section (before `## Solution Structure`):

```markdown
## Internationalization

Book Wheel supports English, Spanish, and Polish.

- A language toggle button (visible on both the login screen and the main app) cycles through the supported languages and persists the choice in the browser's `localStorage`.
- On first visit, the language defaults to the browser's language if it's one of the supported three, otherwise English.
- The frontend sends the selected language as an `Accept-Language` header on every API call, so server-generated error messages (e.g. "Book title is required.") come back already translated.
- Frontend strings live in `BookWheel/wwwroot/js/i18n.js`; backend error-message translations live in `BookWheel/Resources/SharedErrors*.resx`, looked up through `ApiMessageLocalizer`.

### Adding a new language

1. Add the locale code to `SUPPORTED_LOCALES` in `BookWheel/wwwroot/js/i18n.js` and add a full `TRANSLATIONS.<locale>` catalog (copy the `en` object as a starting point — every key must be present or that string falls back to English).
2. Add a `BookWheel/Resources/SharedErrors.<locale>.resx` file with all of the same resource keys as `SharedErrors.resx`.
3. Add the locale code to the `supportedCultures` array in `BookWheel/Program.cs`'s `RequestLocalizationOptions` setup.

Spanish and Polish translations were authored by the assistant as a first pass and have not been reviewed by native speakers — treat them as a solid starting point, not final copy.
```

Also add one line to the existing `## Features` bullet list (near the theme-toggle line) noting the new capability:
```markdown
- Language toggle (English/Spanish/Polish) with saved browser preference and localized server error messages
```

- [ ] **Step 2: Update `IMPROVEMENT_ROADMAP.md`**

Add one bullet to the `## Current Strengths` list (after the theme-support line):
```markdown
- English/Spanish/Polish language support with a persisted user preference and localized server error messages
```

- [ ] **Step 3: Commit**

```bash
git add README.md IMPROVEMENT_ROADMAP.md
git commit -m "Document i18n support in README and roadmap (#28)"
```

---

## Task 12: Version bump and final verification

**Files:**
- Modify: `BookWheel/BookWheel.csproj`

- [ ] **Step 1: Run the full test suite one more time**

Run: `dotnet test BookWheel.slnx`
Expected: PASS, zero failures, across every test file touched in Tasks 1-10.

- [ ] **Step 2: Bump the version**

In `BookWheel/BookWheel.csproj`, change:
```xml
<InformationalVersion Condition="'$(InformationalVersion)' == ''">1.7.0</InformationalVersion>
```
to:
```xml
<InformationalVersion Condition="'$(InformationalVersion)' == ''">1.8.0</InformationalVersion>
```

- [ ] **Step 3: Build to confirm the version stamp takes effect**

Run: `dotnet build BookWheel.slnx`
Expected: builds cleanly with no errors.

- [ ] **Step 4: Commit**

```bash
git add BookWheel/BookWheel.csproj
git commit -m "Bump version to 1.8.0 for I18N support (#28)"
```
